{
  "name": "@aura-sign/next-auth",
  "version": "0.1.0",
  "description": "SIWE authentication handler with iron-session",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "lint": "echo 'No linting configured for next-auth'",
    "test": "echo 'No tests yet for next-auth package'"
  },
  "dependencies": {
    "iron-session": "^8.0.1",
    "siwe": "^2.1.4",
    "ethers": "^6.8.0"
  },
  "peerDependencies": {
    "next": "^13.0.0 || ^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.26",
    "next": "^14.0.0",
    "typescript": "^5.3.0"
  }
}
