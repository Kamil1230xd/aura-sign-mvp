# ==========================================
# Environment Configuration Template
# ==========================================
# Copy this file to .env.local (gitignored) and fill in actual values.
# DO NOT commit real secrets to version control.
#
# To generate secure random secrets, use:
#   openssl rand -base64 32

# ==========================================
# Database Configuration
# ==========================================
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://YOUR_DB_USER:YOUR_DB_PASSWORD@localhost:5432/aura

# Docker Compose Postgres Configuration (for local development)
POSTGRES_USER=YOUR_DB_USER
POSTGRES_PASSWORD=YOUR_DB_PASSWORD
POSTGRES_DB=aura

# ==========================================
# Authentication & Session
# ==========================================
# Application name displayed in SIWE messages
NEXT_PUBLIC_APP_NAME=Aura-Sign-Demo

# Session secret for server-side session management (min 32 characters)
# Generate with: openssl rand -base64 32
SESSION_SECRET=YOUR_SESSION_SECRET_HERE_MIN_32_CHARS

# Iron session password for encrypted cookies (min 32 characters)
# Generate with: openssl rand -base64 32
IRON_SESSION_PASSWORD=YOUR_IRON_SESSION_PASSWORD_HERE_MIN_32_CHARS

# ==========================================
# Object Storage (MinIO)
# ==========================================
MINIO_ENDPOINT=http://localhost:9000

# MinIO credentials (for local development)
MINIO_ROOT_USER=YOUR_MINIO_USER
MINIO_ROOT_PASSWORD=YOUR_MINIO_PASSWORD

# Application-level MinIO access (typically same as root for local dev)
MINIO_ACCESS_KEY=YOUR_MINIO_USER
MINIO_SECRET_KEY=YOUR_MINIO_PASSWORD

# ==========================================
# Queue & Cache (Redis)
# ==========================================
REDIS_URL=redis://localhost:6379

# ==========================================
# Optional Services
# ==========================================
# Embeddings API endpoint
EMBEDDING_API=http://localhost:4001
